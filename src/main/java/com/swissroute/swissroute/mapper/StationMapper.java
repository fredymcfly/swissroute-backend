package com.swissroute.swissroute.mapper;

import com.swissroute.swissroute.dto.RegistroRequest;
import com.swissroute.swissroute.dto.StationRequest;
import com.swissroute.swissroute.dto.api.StationDto;
import com.swissroute.swissroute.entity.Station;
import com.swissroute.swissroute.entity.Usuario;

public class StationMapper {
/*
    public static Station toStation(StationRequest request, Usuario usuario) {
        Station station = new Station();
        station.setStationApiId();
        return usuario;
    }

    private StationDto map(Station station) {
        return new StationDto(
                station.getStationApiId(),
                station.getName()
        );
    }

 */
}
